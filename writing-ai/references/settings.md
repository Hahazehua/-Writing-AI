# Settings

Default Word document folder: `F:\HC\feeling`

Use this folder only when the user does not specify another source folder. Search recursively for `.docx` files and ignore files whose names begin with `~$`.

